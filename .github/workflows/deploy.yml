name: Deploy

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "部署环境"
        required: true
        default: "staging"
        type: choice
        options:
          - staging
          - production
  push:
    tags:
      - "v*"

env:
  REGISTRY: ghcr.io
  BACKEND_IMAGE: ${{ github.repository }}/backend
  FRONTEND_IMAGE: ${{ github.repository }}/frontend

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write # 允许推送镜像

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Normalize image names
        run: |
          echo "BACKEND_IMAGE_LC=$(echo '${{ env.BACKEND_IMAGE }}' | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV
          echo "FRONTEND_IMAGE_LC=$(echo '${{ env.FRONTEND_IMAGE }}' | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: Set version
        run: |
          if [[ "${{ github.ref }}" == refs/tags/v* ]]; then
            echo "DEPLOY_VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_ENV
          else
            echo "DEPLOY_VERSION=latest" >> $GITHUB_ENV
          fi

      - name: Login to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Setup Buf
        uses: bufbuild/buf-setup-action@v1
        with:
          version: ${{ env.BUF_VERSION }}

      - name: Install protoc plugins
        run: |
          go install google.golang.org/protobuf/cmd/protoc-gen-go@latest
          go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@latest
          go install github.com/go-kratos/kratos/cmd/protoc-gen-go-http/v2@latest
          go install github.com/google/gnostic/cmd/protoc-gen-openapi@latest
          echo "$(go env GOPATH)/bin" >> $GITHUB_PATH

      - name: Prepare proto output directory
        run: mkdir -p api

      - name: Generate API code
        working-directory: backend
        run: make api

      # 构建并推送后端镜像
      - name: Build and push backend
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.backend
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.BACKEND_IMAGE_LC }}:${{ env.DEPLOY_VERSION }}

      # 构建并推送前端镜像
      - name: Build and push frontend
        uses: docker/build-push-action@v5
        with:
          context: ./frontend # 改成你前端代码的目录
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.FRONTEND_IMAGE_LC }}:${{ env.DEPLOY_VERSION }}

  deploy:
    needs: build-and-push # 等镜像构建完再部署
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment || 'production' }}

    steps:
      - name: Normalize image names
        run: |
          echo "BACKEND_IMAGE_LC=$(echo '${{ env.BACKEND_IMAGE }}' | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV
          echo "FRONTEND_IMAGE_LC=$(echo '${{ env.FRONTEND_IMAGE }}' | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          envs: REGISTRY,BACKEND_IMAGE_LC,FRONTEND_IMAGE_LC,DEPLOY_VERSION
          script: |
            # 登录镜像仓库（首次需要在服务器上配置好 token）
            docker pull ${{ env.REGISTRY }}/${{ env.BACKEND_IMAGE_LC }}:${{ env.DEPLOY_VERSION }}
            docker pull ${{ env.REGISTRY }}/${{ env.FRONTEND_IMAGE_LC }}:${{ env.DEPLOY_VERSION }}

            # 用 docker compose 重启（推荐）
            cd /path/to/your/app
            DEPLOY_VERSION=${{ env.DEPLOY_VERSION }} docker compose up -d

      - name: Deployment Summary
        run: |
          echo "🚀 部署完成！"
          echo "后端: ${{ env.REGISTRY }}/${{ env.BACKEND_IMAGE_LC }}:${{ env.DEPLOY_VERSION }}"
          echo "前端: ${{ env.REGISTRY }}/${{ env.FRONTEND_IMAGE_LC }}:${{ env.DEPLOY_VERSION }}"
          echo "环境: ${{ github.event.inputs.environment || 'production' }}"
